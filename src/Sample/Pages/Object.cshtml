@page "/objects"

<Dynamic TagName="div" css="@Css1" color="red">this is a nice box.</Dynamic>

<Dynamic TagName="div" css="@Css1" color="blue">this is a nice box.</Dynamic>

@functions {
    //Css Css1 = Css.CreateNew().AddRule("color", "blue").OpenSelector(":hover").AddRule("color", "red").CloseSelector();
    Css Css1 = Css.CreateNew().AddDynamicRule((css, attributes) => { css.AddRule("color", attributes["color"].ToString()); });
}